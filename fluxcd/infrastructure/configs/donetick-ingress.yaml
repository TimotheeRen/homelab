apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: donetick-ingress
  namespace: default
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: donetick
            port:
              number: 2021
